dist
storybook-static
**/*.sass
coverage
node_modules
pnpm-lock.yaml
*.tsbuildinfo
.beads
CHANGELOG.md

# Isolated reviewer worktrees — full copies of this repo inside it. Not a
# theoretical exposure: `prettier --check .` walks them, and it passes today
# only because those copies happen to be conformant. A reviewer mid-edit would
# fail format:check on the MAIN checkout, with no cause visible in the diff.
#
# NOT the whole of `.claude/`: settings.json, the agent briefs and the skills
# are tracked and were being checked. Ignoring the parent hid them.
.claude/worktrees/
